## Test `renv` project

-   Open `renv_project.Rproj`
    -   Does it open RStudio without problems? (No crash)
    -   Read the `renv` startup message. Follow instructions.

-   Run the code in `script.R`
    -   Does it run without issues?
